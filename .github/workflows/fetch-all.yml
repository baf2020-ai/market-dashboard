name: Fetch All Data (Manual)

on:
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd scripts && npm ci

      - name: Fetch US market data
        env:
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
        run: cd scripts && node fetch-us-market.mjs

      - name: Fetch US news
        env:
          FINLIGHT_API_KEY: ${{ secrets.FINLIGHT_API_KEY }}
        run: cd scripts && node fetch-us-news.mjs

      - name: Fetch KR market data
        run: cd scripts && node fetch-kr-market.mjs

      - name: Fetch asset prices
        run: cd scripts && node fetch-assets.mjs

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/
          git diff --staged --quiet || git commit -m "data: update all market data $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
